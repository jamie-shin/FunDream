<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="dao.IProjectDao">

	<select id="selectAllProjects" parameterType="project">
		select * from Project
	</select>
	
	<select id="selectEndProject" resultType="project">
	<![CDATA[
		select * from project where p_startdate <= now() and p_enddate >= now() and p_approval=2 order by p_enddate limit 3
		]]>
	</select>

	<select id="selectNewProject" resultType="project">
		<![CDATA[
		select * from project where p_startdate <= now() and p_approval=2 order by p_startdate desc limit 3
		]]>
	</select>
	
	<select id="selectSuccessProject" resultType="project">
	<![CDATA[
		select * from project where p_target <= p_status order by p_startdate desc limit 3
		]]>
	</select>
</mapper>